# Evaluation Reports

Run the evaluation from the `backend` directory:

```powershell
..\.venv\Scripts\python.exe -m evaluation.run_evaluation
```

The command writes `latest.json` and `latest.md` here. Reports contain aggregate
metrics and case identifiers, not resume text or API keys.
